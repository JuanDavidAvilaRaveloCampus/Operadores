// OPERADORES ANS
let tabla = {
    "NOMBRE" : "DESCRIPCIÓN",
    "Math.trunc(x)" : "Remueve digitos decimales"
};
console.table(tabla);

// EJEMPLO
console.log("Ejemplo con Math.trunc()...Removiendo los decimales de N. Siendo N = 1.49, entonces Math.trunc(N) = " + Math.trunc(1.49));

// EJEMPLO 2
console.log("Ejemplo 2 con Math.trunc()...Removiendo los decimales de N. Siendo N = 5.5856, entonces Math.trunc(N) = " + Math.trunc(5.5856));